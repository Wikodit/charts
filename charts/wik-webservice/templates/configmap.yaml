{{- define "configmap" -}}
{{ $rangeItem := index . 0 }}
{{ with index . 1 }}
apiVersion: v1
kind: ConfigMap
metadata:
{{ if $rangeItem.name}}
  name: "{{ template "fullname" . }}-{{ $rangeItem.name }}"
{{ else }}
  name: "{{ template "fullname" . }}"
{{ end }}
  labels:
    app: {{ template "name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
data:
{{ range $key, $val := $rangeItem.env.plaintext }}
  {{ $key }}: {{ $val | quote }}
{{ end }}
{{ end }}
{{- end -}}

{{ $root := . }}
{{ if .Values.webservice.env.plaintext }}
{{ template "configmap" (list .Values.webservice $) }}
{{ end }}

{{ range .Values.webservice.additionalContainers }}
{{ if and .env .env.plaintext }}
---
{{ template "configmap" (list . $) }}
{{ end }}
{{ end }}

{{ range .Values.webservice.initContainers }}
{{ if and .env .env.plaintext }}
---
{{ template "configmap" (list . $) }}
{{ end }}
{{ end }}