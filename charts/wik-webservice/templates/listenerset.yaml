{{ $root := . }}
{{- if .Values.webservice.httpRoute.tls -}}
{{- if .Capabilities.APIVersions.Has "gateway.networking.k8s.io/v1/ListenerSet" }}
apiVersion: gateway.networking.k8s.io/v1
kind: ListenerSet
{{- else }}
apiVersion: gateway.networking.x-k8s.io/v1alpha1
kind: XListenerSet
{{- end }}
metadata:
  name: {{ template "fullname" . }}
  labels:
    app: {{ template "name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
    {{- range $key, $value := (.Values.webservice.listenerSet.labels | default dict) }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
{{- with .Values.webservice.listenerSet.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  parentRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    {{- if .Values.webservice.listenerSet.gatewayRef.name }}
    name: {{ .Values.webservice.listenerSet.gatewayRef.name }}
    {{- with .Values.webservice.listenerSet.gatewayRef.namespace }}
    namespace: {{ . }}
    {{- end }}
    {{- else if .Values.webservice.httpRoute.parentRefs }}
    name: {{ (index .Values.webservice.httpRoute.parentRefs 0).name }}
    {{- with (index .Values.webservice.httpRoute.parentRefs 0).namespace }}
    namespace: {{ . }}
    {{- end }}
    {{- else }}
    {{- fail "httpRoute.tls requires either listenerSet.gatewayRef.name or httpRoute.parentRefs to be set" }}
    {{- end }}
  listeners:
    {{- range .Values.webservice.hosts }}
    - name: {{ . | replace "." "-" }}
      hostname: {{ . | quote }}
      port: {{ $root.Values.webservice.listenerSet.port }}
      protocol: {{ $root.Values.webservice.listenerSet.protocol }}
      tls:
        mode: {{ $root.Values.webservice.listenerSet.tls.mode }}
        certificateRefs:
        {{- if $root.Values.webservice.listenerSet.tls.certificateRefs }}
{{ toYaml $root.Values.webservice.listenerSet.tls.certificateRefs | indent 10 }}
        {{- else }}
          - kind: Secret
            name: {{ template "fullname" $root }}--tls
        {{- end }}
    {{- end }}
{{- end }}
