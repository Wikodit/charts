Your {{ .Chart.Name }} chart has been installed successfully!

NodeLocal DNS has been deployed to improve DNS resolution performance.

To check the status of the NodeLocal DNS daemonset, run:

  kubectl get daemonset {{ .Release.Name }}-{{ .Chart.Name }} -n kube-system

To verify DNS pods are running on each node:

  kubectl get pods -n kube-system -l app.kubernetes.io/name={{ .Chart.Name }},app.kubernetes.io/instance={{ .Release.Name }}

To check DNS configuration, you can test with:

  nslookup kubernetes.default.svc.cluster.local

For more information on using the chart, see the documentation at:
https://github.com/wikodit/charts/tree/main/charts/{{ .Chart.Name }}
